import styled from "styled-components"
import { Container_main } from "../../styled/generalStyled"

const ItemsMain = styled(Container_main).attrs({as: "section"})`
  width: 100%;
  padding: 9rem 0;
`

export { ItemsMain }